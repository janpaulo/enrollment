<p id="notice"><%= notice %></p>

<h1>Subject Teachers</h1>

<table>
  <thead>
    <tr>
      <th>Teacher</th>
      <th>Subject</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subject_teachers.each do |subject_teacher| %>
      <tr>
        <td><%= subject_teacher.teacher %></td>
        <td><%= subject_teacher.subject %></td>
        <td><%= link_to 'Show', subject_teacher %></td>
        <td><%= link_to 'Edit', edit_subject_teacher_path(subject_teacher) %></td>
        <td><%= link_to 'Destroy', subject_teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject Teacher', new_subject_teacher_path %>
