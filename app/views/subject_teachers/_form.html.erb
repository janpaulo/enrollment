<%= form_with(model: subject_teacher, local: true) do |form| %>
  <% if subject_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_teacher.errors.count, "error") %> prohibited this subject_teacher from being saved:</h2>

      <ul>
      <% subject_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
