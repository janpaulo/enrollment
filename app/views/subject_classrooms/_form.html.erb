<%= form_with(model: subject_classroom, local: true) do |form| %>
  <% if subject_classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_classroom.errors.count, "error") %> prohibited this subject_classroom from being saved:</h2>

      <ul>
      <% subject_classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :classroom_id %>
    <%= form.text_field :classroom_id %>
  </div>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
